
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Open rules for development. Secure before deploying to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
