# Arquivo de configuração para Firebase Data Connect (opcional).
# Deixado vazio intencionalmente pois não estamos usando Data Connect diretamente neste projeto.
# Este arquivo existe para silenciar avisos do Firebase CLI.

# Exemplo de como seria se fosse usado:
# serviceId: my-sql-service
# source:
#   mysql:
#     connectionString: MYSQL_CONNECTION_STRING_ENV_VAR
# connectors:
#   - type: OPENAPI
#     location: openapi.yaml
# allowedOrigins:
#   - https://your-app.web.app
#
# Schema Definition for platformSettings (Conceptual - for documentation)
# This structure will be implemented directly in SQL adapter schemas.

# platformSettings:
#   fields:
#     id:
#       type: string
#       primaryKey: true
#       description: Unique identifier for platform settings (e.g., 'default')
#     activeThemeName:
#       type: string
#       description: The name of the currently active theme
#     themes:
#       type: array
#       items:
#         type: object
#         properties:
#           name:
#             type: string
#             description: Name of the color theme (e.g., 'Default', 'Dark', 'Ocean Blue')
#           colors:
#             type: object
#             description: Key-value pairs of color variables (e.g., primary: '#...', background: 'hsl(...)')
#             additionalProperties: { type: string }
#   - http://localhost:3000
