// prisma/models/map-settings.prisma

model MapSettings {
  id                      BigInt           @id @default(autoincrement())
  settings                PlatformSettings @relation(fields: [settingsId], references: [id], onDelete: Cascade)
  settingsId              BigInt           @unique

  defaultProvider         BigInt  @default("openstreetmap") // 'google', 'openstreetmap', 'staticImage'
  googleMapsApiKey        BigInt?
  staticImageMapZoom      Int     @default(15)
  staticImageMapMarkerColor BigInt  @default("blue")

  @@map("map_settings")
}
