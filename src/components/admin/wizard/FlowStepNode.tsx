// src/components/admin/wizard/FlowStepNode.tsx
// Este componente foi desativado pois era usado exclusivamente pelo `reactflow`.
// O conteúdo foi removido para evitar a manutenção de código não utilizado.

import React from 'react';

// O conteúdo original do componente foi removido pois `reactflow` foi desinstalado.
const FlowStepNode = () => {
    return null;
};

export default FlowStepNode;
