/**
 * @fileoverview Página administrativa para edição do próprio perfil do usuário.
 * Renderiza o formulário de edição dentro do layout do painel (`/dashboard`).
 */

import EditProfilePageContent from '@/components/profile/edit-profile-page';

export default function DashboardEditProfilePage() {
  return <EditProfilePageContent />;
}