/**
 * Tipos para as linhas da tabela de Subscriber no Admin Plus.
 */
export interface SubscriberRow {
  id: string;
  email: string;
  name: string;
  phone: string;
  preferences: string;
  createdAt: string;
  updatedAt: string;
}
