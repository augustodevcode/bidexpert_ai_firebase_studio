-- Este arquivo foi substituído pela lógica de alteração programática em /scripts/init-db.ts
-- para lidar com a falta de 'IF NOT EXISTS' em `ADD COLUMN` no MySQL.
-- O script em `init-db.ts` agora verifica se cada coluna existe antes de tentar adicioná-la.
-- Manter este arquivo vazio evita a execução de SQL potencialmente problemático.

